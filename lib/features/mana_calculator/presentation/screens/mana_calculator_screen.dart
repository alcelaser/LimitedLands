import 'package:flutter/material.dart';

class ManaCalculatorScreen extends StatelessWidget {
  const ManaCalculatorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mana Calculator - Coming Soon'),
    );
  }
}
